- Date();// 코드를 실행한 시점의 시간, 분, 초 등을 알려줌
: 변수선언방법 const date = new Date();

- padStart (2, "0") // 2글자 이하일 경우 글자 앞쪽에 0을 채워넣는다. 
(padStart (10, "바보") 로 작성 시 10글자 아래일 경우 바보가 반복 출력, (ex, 바보바 10글자이하 ))

- padEnd (2, "0") // 2글자 이하일 경우 글자 뒤쪽에 0을 채워넣는다. 
(padEnd (10, "바보") 로 작성 시 10글자 이하일 경우 바보가 반복 출력, (ex, 10글자이하 바보바))


- String() // ()안에 원하는 데이터, 함수 넣으면 스트링타입 데이터로 변환할 수 있음

- setInterval(getclock, 1000);// 1초마다 호출(반복)


-round //반올림
- ceil // 무조건 올림
- floor // 무조건 내림
